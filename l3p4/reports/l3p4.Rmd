---
title: "L3P4: p-valores vs ICs"
author: "Rebeca Miranda e Thiago Montenegro"
date: "04 de Julho de 2019"
output: html_notebook:
        df_printed: paged
theme: sandstone
---

```{r echo = FALSE, message = FALSE}
library(tidyverse)
theme_set(theme_bw())

library(boot)
library(broom)

knitr::opts_chunk$set(tidy = FALSE,
                      fig.width = 6,
                      fig.height = 5)

```

#Queremos observar .................

```{r}
dados = read.csv2(here::here("data/campeonato-brasileiro.csv"))

```

##1. Jogos aos domingos têm mais gols que jogos às quartas? 
# Calculando o Intervalo de Confiança

```{r}
jogo_dom = dados %>% 
  filter(Dia == "Domingo") %>% 
  summarise(mediana_gols = median(gols))
```


```{r}
theta_f = function(d, i){
    d %>% 
        slice(i) %>% 
        summarise(correlacao = cor(, seguido_gh)) %>% 
        pull(correlacao)
}

theta_c = theta_f(dados, 1:NROW(dados))
```




#2. Jogos onde há um vitorioso têm mais ou menos gols que empates? 
